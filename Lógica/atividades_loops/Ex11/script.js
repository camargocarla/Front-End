/*Escreva um script que verifica se 
uma string é vazia ou não e exibe a mensagem correspondente.*/

let aString = "";

for (let i = 0; i < 1; i++) {
  if (aString === "") {
    document.write("A string está vazia.");
  } else {
    document.write("A string não está vazia.");
  }
}
